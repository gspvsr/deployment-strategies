apiVersion: apps/v1 
kind: Deployment
metadata:
  name: blue-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      name: blue-deployment
  template:
    metadata:
      labels:
        name: blue-deployment
    spec: 
      containers:
      - name: blue-deployment
        image: adamtravis/rollouts:blue
        # blue, green, Orange
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP

---
apiVersion: v1
kind: Service
metadata: 
  name: blue-deployment
  labels:
    name: blue-deployment
spec:
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
  selector:
    name: green-deployment
  type: LoadBalancer
